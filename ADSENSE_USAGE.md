# 📢 Google AdSense - Інструкція по використанню

## 🎯 Два способи додавання оголошень:

### 1. **Auto Ads** (Автоматичні оголошення) - РЕКОМЕНДОВАНО

Google сам вибирає де розміщувати оголошення на всіх сторінках.

#### Додати один раз в `layout.tsx`:

```tsx
import AdSenseAutoAds from "@/components/AdSenseAutoAds";

export default function RootLayout({ children }) {
  return (
    <html lang="fr">
      <head>
        {/* ... інші мета-теги ... */}
      </head>
      <body>
        <AdSenseAutoAds /> {/* ✅ Додати тут */}
        {children}
      </body>
    </html>
  );
}
```

---

### 2. **Manual Ads** (Ручне розміщення) - для контролю

Розміщуєш оголошення вручну в потрібних місцях.

#### Використання компонента `AdSense`:

```tsx
import AdSense from "@/components/AdSense";

export default function TasksPage() {
  return (
    <div>
      <h1>Завдання</h1>
      
      {/* Оголошення після заголовка */}
      <AdSense 
        adSlot="1234567890" 
        adFormat="auto"
        className="my-4"
      />
      
      <div className="tasks-list">
        {/* ... список завдань ... */}
      </div>
      
      {/* Оголошення в кінці */}
      <AdSense 
        adSlot="0987654321" 
        adFormat="horizontal"
        className="my-8"
      />
    </div>
  );
}
```

---

## 📋 Типи оголошень (adFormat):

| Format | Опис | Використання |
|--------|------|--------------|
| `auto` | Автоматичний розмір | Універсальне (рекомендовано) |
| `fluid` | Адаптивне | Для контента різної ширини |
| `rectangle` | Прямокутник | Бічна панель, в контенті |
| `vertical` | Вертикальний | Бічна панель |
| `horizontal` | Горизонтальний | Верх/низ сторінки |

---

## 🔑 Отримати Ad Slot ID:

1. Заходиш в [Google AdSense](https://www.google.com/adsense)
2. **Оголошення** → **За блоком оголошень** → **Створити блок**
3. Вибираєш тип (Display ads)
4. Копіюєш **data-ad-slot** ID (10 цифр)
5. Використовуєш в компоненті:

```tsx
<AdSense adSlot="1234567890" />
```

---

## 🎨 Приклади розміщення:

### В блозі (між параграфами):

```tsx
export default function BlogPost({ content }) {
  return (
    <article>
      <h1>Заголовок статті</h1>
      
      <p>{content.intro}</p>
      
      {/* Оголошення після вступу */}
      <AdSense adFormat="rectangle" className="my-6" />
      
      <p>{content.body}</p>
      
      {/* Оголошення в кінці */}
      <AdSense adFormat="horizontal" className="mt-8" />
    </article>
  );
}
```

### На сторінці завдань:

```tsx
export default function TasksBrowse({ tasks }) {
  return (
    <div>
      <h1>Перегляд завдань</h1>
      
      {/* Оголошення зверху */}
      <AdSense adFormat="auto" className="mb-6" />
      
      <div className="grid grid-cols-3 gap-4">
        {tasks.map((task, idx) => (
          <>
            <TaskCard key={task.id} task={task} />
            
            {/* Оголошення кожні 6 карток */}
            {(idx + 1) % 6 === 0 && (
              <div className="col-span-3">
                <AdSense adFormat="horizontal" className="my-4" />
              </div>
            )}
          </>
        ))}
      </div>
    </div>
  );
}
```

### Бічна панель (sidebar):

```tsx
export default function Layout({ children }) {
  return (
    <div className="flex gap-4">
      <main className="flex-1">{children}</main>
      
      <aside className="w-72">
        <h3>Рекламні пропозиції</h3>
        
        {/* Вертикальне оголошення */}
        <AdSense 
          adFormat="vertical" 
          adSlot="1234567890"
          className="sticky top-4"
        />
      </aside>
    </div>
  );
}
```

---

## ⚠️ Важливі правила Google AdSense:

### ✅ МОЖНА:
- Розміщувати оголошення в контенті
- Використовувати Auto Ads для простоти
- Поєднувати Auto Ads + Manual Ads
- Адаптивні оголошення (responsive)

### ❌ ЗАБОРОНЕНО:
- Більше 3 оголошень на екрані одночасно
- Оголошення над головним контентом (до першого scroll)
- Кліки по власних оголошеннях
- Штучне збільшення кліків
- Оголошення на сторінках без контенту

---

## 🚀 Швидкий старт (Auto Ads):

1. **Оновити `layout.tsx`:**

```tsx
import AdSenseAutoAds from "@/components/AdSenseAutoAds";

export default function RootLayout({ children }) {
  return (
    <html lang="fr">
      <body>
        <AdSenseAutoAds />
        {children}
      </body>
    </html>
  );
}
```

2. **Зібрати та задеплоїти:**

```bash
npm run build
pm2 restart prochepro-frontend
```

3. **Перевірити в Google AdSense:**
   - Зайди в AdSense dashboard
   - Чекай 24-48 годин для появи оголошень
   - Перевір статус верифікації сайту

---

## 🔍 Перевірка роботи:

### В браузері:

1. Відкрий DevTools (F12)
2. Console → перевір помилки AdSense
3. Network → фільтр "pagead" → перевір запити до Google
4. Elements → знайди `<ins class="adsbygoogle">`

### На продакшені:

```javascript
// В консолі браузера на сайті
console.log(window.adsbygoogle);
// Має показати масив
```

---

## 📊 Аналітика:

Статистика в Google AdSense з'явиться через:
- **Перші кліки:** 24-48 годин
- **Повна статистика:** 2-3 дні
- **Перший платіж:** коли накопичиться $100

---

## 🛠️ Troubleshooting:

### Оголошення не з'являються?

1. **Перевір верифікацію сайту** в AdSense
2. **Зачекай 24-48 годин** після активації
3. **Очисти кеш** браузера (Ctrl+Shift+R)
4. **Перевір AdBlock** - вимкни його

### Помилка в консолі?

```
adsbygoogle.push() error: No slot size for availableWidth=0
```

**Рішення:** Переконайся що контейнер має ширину:

```tsx
<div style={{ minHeight: "250px", minWidth: "300px" }}>
  <AdSense />
</div>
```

---

## 📝 Підсумок:

- ✅ **Скрипт AdSense вже доданий** в `layout.tsx`
- ✅ **ID:** `ca-pub-7434166826056099`
- 🎯 **Рекомендація:** Використай Auto Ads для початку
- 📈 Після накопичення статистики - додай Manual Ads в ключових місцях

**Готово! Оголошення з'являться після затвердження Google (24-48 год).** 🎉
